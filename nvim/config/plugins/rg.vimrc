" Settings for RG integration with vim

"let g:rg_binary='/home/sam/bin/ripgrep/rg'

" Setting this will get Rg to interpet the source tree and figure out where
" the project root is... Quite clever!
"let g:rg_derive_root=1
